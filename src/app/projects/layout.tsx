import React from 'react';

export const metadata = {
  title: 'mhmmd azmi - Projects',
  description: 'Portfolio Website',
};

export default function layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
