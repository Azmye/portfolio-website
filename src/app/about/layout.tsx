import React from 'react';

export const metadata = {
  title: 'mhmmd azmi - About',
  description: 'Portfolio Website',
};

export default function layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
